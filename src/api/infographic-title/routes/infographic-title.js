'use strict';

/**
 * infographic-title router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::infographic-title.infographic-title');
