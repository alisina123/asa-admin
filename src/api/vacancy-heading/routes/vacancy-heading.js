'use strict';

/**
 * vacancy-heading router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::vacancy-heading.vacancy-heading');
