'use strict';

/**
 * imge-slider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::imge-slider.imge-slider');
