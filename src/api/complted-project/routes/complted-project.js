'use strict';

/**
 * complted-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::complted-project.complted-project');
