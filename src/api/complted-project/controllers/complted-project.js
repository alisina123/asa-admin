'use strict';

/**
 * complted-project controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::complted-project.complted-project');
