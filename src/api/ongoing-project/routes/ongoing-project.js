'use strict';

/**
 * ongoing-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ongoing-project.ongoing-project');
