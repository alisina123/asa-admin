'use strict';

/**
 * ongoing-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ongoing-project.ongoing-project');
