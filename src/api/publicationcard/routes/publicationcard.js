'use strict';

/**
 * publicationcard router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::publicationcard.publicationcard');
