'use strict';

/**
 * planned-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::planned-project.planned-project');
