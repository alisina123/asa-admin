'use strict';

/**
 * planned-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::planned-project.planned-project');
